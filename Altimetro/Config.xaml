<Page
    x:Class="Altimetro.Config"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Altimetro"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="3*" />
            <ColumnDefinition Width="2*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="1*" />
            <RowDefinition Height="5*"/>
        </Grid.RowDefinitions>
                <AppBar Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="3">
            <AppBarButton Icon="Home" Label="Home" Click="AppBar_Click"/>
        </AppBar>
        <TextBox Grid.Column="0" Grid.Row="1" HorizontalAlignment="Left" Margin="12,190,0,0" TextWrapping="Wrap" Text="Temperature (°K)" VerticalAlignment="Top" FontSize="16"/>
        <TextBox x:Name="Temp" Grid.Column="1" Grid.Row="1" HorizontalAlignment="Right"   Margin="12,190,0,0" TextWrapping="Wrap" Text="290" VerticalAlignment="Top" FontSize="16"/>
    </Grid>
</Page>
